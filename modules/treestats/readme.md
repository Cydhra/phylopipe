Provides access to the treestats package via R.
The treestats package calculates several tree metrics.
Only some of the metrics are exposed as Powershell functions, but the module is easily
extensible.
Installation forces installation of the conda and rscript module.